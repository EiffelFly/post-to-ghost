{
  "name": "post-to-ghost",
  "version": "0.0.1",
  "description": "Post to Ghost instance once target folder have new post",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/action.ts -o dist"
  },
  "author": "Summerbud",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.8",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "@actions/glob": "^0.2.0",
    "@vercel/ncc": "^0.33.1",
    "node-fetch": "^3.1.0"
  }
}
